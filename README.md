# Shortest Path Finder Using BFS and A*
This repository contains a Java implementation for finding the shortest path in an unweighted graph using the Breadth-First Search (BFS) algorithm, and in a weighted graph using the A* algorithm. Both algorithms are widely used for pathfinding and graph traversal.

# Overview
The project provides two pathfinding algorithms:

1- Breadth-First Search (BFS): Used for finding the shortest path in an unweighted graph. BFS explores nodes level by level, ensuring that the first time a node is reached, it's reached with the shortest path.

2- A*: Used for finding the shortest path in a weighted graph. A* improves on Dijkstra's algorithm by using heuristics to prioritize nodes that are closer to the destination, making it more efficient in many cases.

# Features
 - Graph Representation: Graphs are represented as adjacency lists. The A* implementation supports weighted edges.
 - Pathfinding Algorithms: Includes implementations of BFS and A* for different types of graphs.
 - Path Reconstruction: Both algorithms reconstruct and display the shortest path from the source node to the destination node.
# ScreenShots For the Project
 ## ![image](https://github.com/user-attachments/assets/75f5f5a9-a6d2-47c2-90e3-373b341f25cc)
 ## Photo for A Algorithm* Button Click:
  - Clicking the 'A' button initiates the A* pathfinding algorithm, which calculates the shortest path from the source to the destination using heuristics and displays the path on the map. 
    ![image](https://github.com/user-attachments/assets/dae1d6e5-05d6-40d8-869f-8e5a776921f2)
 ## Photo for BFS Algorithm Button Click:
   - Clicking the 'BFS' button initiates the Breadth-First Search algorithm to find the shortest path in an unweighted graph from the source to the destination and displays the path on the map.
    ![image](https://github.com/user-attachments/assets/e0c5dbb1-3331-4d5a-b2f4-5060d04f1a35)

